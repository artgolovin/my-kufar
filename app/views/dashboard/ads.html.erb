<div class="row text-center">
  <div class="col">
    <h1>My <%= @adver_status %> advertisements<h1>
  </div>
</div>

<%= render partial: 'shared/adver/show', collection: @ads, as: :adver %>

<%= paginate @ads %>
